## 1. Route Inventory

- [x] 1.1 Inventory active navigation targets from the app shell and key page-level menus.
- [x] 1.2 Compare the inventory against the active route table and classify mismatches as supported, redirect, or removal.

## 2. Contract Alignment

- [x] 2.1 Update route definitions and navigation links so active paths resolve consistently under the selected baseline.
- [x] 2.2 Add explicit redirect aliases only where legacy URLs still need to remain reachable.

## 3. Navigation Verification

- [x] 3.1 Verify marketing-shell navigation flows reach the intended pages.
- [x] 3.2 Verify CMS and user-center navigation flows reach the intended pages without unintended catch-all redirects.
